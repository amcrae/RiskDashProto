<%= form_with(model: m_location) do |form| %>
  <% if m_location.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(m_location.errors.count, "error") %> prohibited this m_location from being saved:</h2>

      <ul>
        <% m_location.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
